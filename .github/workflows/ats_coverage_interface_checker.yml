name: ats_coverage_interface_checker

permissions:
  contents: read

on:
  push:
    branches: [ dev ]
    paths:
      - 'ats_coverage/**/*.py'
      - 'setup.py'
  pull_request:
    branches: [ dev ]
    paths:
      - 'ats_coverage/**/*.py'
      - 'setup.py'

jobs:
  interface_gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Verify that each concrete class implements a valid interface
        run: |
          python3 gates/gates/interfaces_checker.py .
