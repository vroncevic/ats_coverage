name: ats_coverage_srp_checker

permissions:
  contents: read

on:
  push:
    branches: [ dev ]
    paths:
      - 'ats_coverage/**/*.py'
      - 'setup.py'
  pull_request:
    branches: [ dev ]
    paths:
      - 'ats_coverage/**/*.py'
      - 'setup.py'

jobs:
  srp_gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Check Single Responsibility Principle violations
        run: |
          python3 gates/gates/srp_checker.py .
