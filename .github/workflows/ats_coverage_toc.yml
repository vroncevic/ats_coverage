name: ats_coverage_toc
on:
  push:
    branches: [ main ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: ats_coverage_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
